package com.example.simpleapplication.something;

public class Unused {

    public void something() {
        for (int i = 0; i < 10; i++) {
            long toss = Math.round(Math.random());
            System.out.printf("Coin toss: %d%n", toss);
        }
    }
}
